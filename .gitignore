# Dependencies
node_modules/

# Editor / OS junk
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/

# Build output is committed so users can load `dist/` straight into
# chrome://extensions without running `npm install` / `npm run build`.
# To regenerate after source changes: `npm run build`.
# dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local env
.env
.env.local
